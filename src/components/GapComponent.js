import React from 'react';
import { View } from 'react-native';
export const GapHorizontal = ({ width }) => {
  return <View style={{ width: width }} />;
};
export const GapVertical = ({ height }) => {
  return <View style={{ height: height }} />;
};
